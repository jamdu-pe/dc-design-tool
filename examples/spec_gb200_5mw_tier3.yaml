project: "AI DC 개념설계 - GB200 5MW Tier III"
it_power_mw: 5.0
rack_id: nvidia_gb200_nvl72
tier: "III"
electrical_redundancy: "2N"
mechanical_redundancy: "N+1"
chw_delta_t_k: 10
target_pue: 1.25
ambient_design_c: 33
climate: "KR-Busan"

# 장비를 바꾸려면 역할(subtype) → block_id 를 적는다. 안 적은 역할은 `default: true`가
# 붙은 블록을 쓴다.
# 쓸 수 있는 id 는 `dc-design catalog --type electrical --subtype ups` 로 확인한다.
# selections:
#   ups: ups_vertiv_exl_s1_800kva
#   cdu: cdu_coolit_chx1000
